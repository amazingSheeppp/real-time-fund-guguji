# 咕咕鸡 (Guguji) 开发计划

> 本文档基于产品讨论结论整理,是「实时估值自选列表 + 圈子(邮箱登录/持仓同步)+ 强更」的完整开发路线图。
> UI 风格已定稿:墨水屏(Kindle)风格,规范见 `design/DESIGN_SPEC.md`,效果图 `design/home_eink.html`。
> 共分两期,每期按可独立交付的小步拆分,并标注依赖顺序与验收点。

---

## 一、产品与技术方案总览(结论回顾)

| 项目 | 结论 |
|------|------|
| 产品形态 | 一期:纯客户端「实时估值自选列表」;二期:接入自建后端加「圈子」 |
| App 结构 | 3 Tab:自选 / 圈子 / 我的(设置) |
| Tab 1 · 自选 | 实时估值列表,客户端直连东方财富公开接口,30s 自动刷新 + 下拉刷新,涨跌用墨色浓淡区分 |
| Tab 2 · 圈子 | 邮箱登录后展示「我的持仓」(可增删)+「朋友持仓」(按朋友分组,真实持仓) |
| Tab 3 · 我的 | 设置、清除数据、关于 |
| UI 风格 | 墨水屏(Kindle)风格:中性灰阶、墨分五色替代红绿、衬线标题、无彩色、刷新闪烁动效 |
| 登录 | 邮箱 + 验证码注册登录,自建后端 SMTP 发码(零成本) |
| 圈子 | 圈子码加入 + 创建人审批(公开可申请) |
| 同步方式 | onResume + 下拉刷新,纯 REST,无 WebSocket |
| 后端 | Python + FastAPI + SQLAlchemy + MySQL + JWT + SMTP,代码放本仓库 `server/` 子目录 |
| 强更 | 应用内下载 APK(版本检查 + 强制更新框 + FileProvider + 安装引导) |
| 估值数据流 | 客户端直连东方财富公开接口拉估值,服务器只负责用户/圈子/持仓与 App 版本,不碰行情 |

---

## 二、一期:纯客户端——实时估值自选列表(单机可用)

> 目标:一个能搜、能加、能删、能实时刷新的自选基金列表 App,先砍掉持仓/盈亏,立住核心。

### 1-1 移除持仓与盈亏模块

- 删:持仓实体/DAO/表、编辑持仓弹窗(HoldingEditDialog)、收益计算器(ProfitCalculator)、FundEntity 上的 profit 字段
- 改:刷新流程(去掉算收益分支)、Home 顶部汇总区改纯估值展示、仓库/ViewModel 清理持仓方法
- ✅ 验收:App 可编译运行,自选列表只显示名称/代码/估值净值/涨跌幅;无持仓相关引用残留

### 1-2 墨水屏 UI 落地

- 按 `design/DESIGN_SPEC.md` 落地 `colors.xml` / `themes.xml` / drawable(卡片/徽标/Tab/FAB/印章)
- 字体:标题用衬线(思源宋体),数字用等宽
- 涨跌语义:涨=浓墨实心徽标,跌=淡墨空心徽标,停牌=虚线
- 移除顶部盈亏汇总区,改为纯估值展示(市场状态 + 更新时间)
- ✅ 验收:界面视觉与效果图一致,红涨绿跌改为墨色浓淡

### 1-3 自选核心体验打磨

- 搜索添加、长按删除、下拉刷新、30s 自动刷新、空态页
- 列表排序(按涨跌幅/按添加时间)、估值时间展示
- ✅ 验收:单机可用,数据来自东方财富公开接口

### 1-4 收尾

- 应用图标、命名、首启体验、基础崩溃兜底
- ✅ 验收:可打 APK 安装使用

---

## 三、二期:接入自建后端——登录 + 圈子

> 目标:邮箱注册登录 + 我的持仓 + 朋友持仓分组展示 + 强更。
> 分后端、客户端、强更三条线推进。

### 阶段 A · 后端(Python + FastAPI + MySQL)

| 步骤 | 内容 | 验收 |
|------|------|------|
| A1 基建 | FastAPI 骨架、MySQL(SQLAlchemy)、配置,放 `server/` | 服务可启动,数据库连通 |
| A2 用户系统 | 用户表 + 邮箱验证码(SMTP)+ 注册/登录 + JWT + 鉴权中间件 | 注册登录调通,Token 生效 |
| A3 圈子系统 | 圈子表、成员表(审批状态)、创建/加入/审批/退出 | 审批流调通 |
| A4 持仓系统 | 持仓表 + 增删改查,按圈子/用户聚合查询 | CRUD 与聚合正确 |
| A5 App 版本 | `/api/app/version`(版本号、最低强制版本、APK 下载、说明) | 接口返回正确 |

### 阶段 B · 客户端(Android)

| 步骤 | 内容 | 验收 |
|------|------|------|
| B1 网络层 | Retrofit/OkHttp 封装 + Token 存储(登录态) | 请求带 Token,登录态持久 |
| B2 登录页 | 邮箱 + 验证码注册登录 UI(墨水屏风格) | 可注册登录 |
| B3 圈子 Tab | 登录态检查、「我的持仓」增删、「朋友持仓」分组展示,onResume + 下拉刷新 | 双端互见持仓 |
| B4 强更 | 版本检查 + 无取消强制更新框 + APK 下载 + FileProvider + 安装引导 | 强更全流程可用 |

### 阶段 C · 联调与发布

| 步骤 | 内容 | 验收 |
|------|------|------|
| C1 端到端联调 | 双客户端互见持仓、审批流、强更全流程 | 全流程跑通 |
| C2 打包发布 | 签名 APK、上传(蒲公英/酷安)、强更从该链接分发 | 朋友能安装、能加圈子、互看持仓、强更生效 |

---

## 四、依赖顺序

```
一期(1-1 → 1-2 → 1-3 → 1-4)
        └──> 二期 A 后端(可并行先起 A1-A2)
                  └──> B 客户端(依赖 A 的接口定稿)
                            └──> C 联调发布
```

**关键提示**:A1–A2(后端基建 + 用户系统)可与一期并行推进;但 B(客户端圈子)需等 A 的接口定稿。

---

## 五、需提前准备的东西

- **域名 + 境内服务器**:App 备案、部署后端、上架要用
- **可用的发信邮箱**:后端 SMTP 发验证码用(QQ/163 免费邮箱即可)
- **Python 环境**:开发期本地即可
- **软著 + App 备案**(若后续正式上架大厂市场):周期约 2–3 个月,建议尽早启动;不上架则用蒲公英/酷安分发

---

## 六、风险与注意点

- **强更的应用内 APK**:需处理 Android 8+ 未知来源安装授权、FileProvider、APK 签名一致性(签名变了会导致无法覆盖安装)
- **墨水屏无彩色**:涨跌依赖墨色浓淡,需保证对比度与可辨识度(规范见 DESIGN_SPEC)
- **后端安全**:JWT 密钥、验证码防刷(限流)、Token 过期与刷新
